#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#define SIZE 10
int convert(int k)
{
  return k%10;
}
void insert(int h[],int key)
{
  int index;
  index=convert(key)
  if(h[index]!)
   h[index]=key;
   else
   i=1;
   while((index+i)%10!)
   h[(index+i)%10]=key;
   i++;


}
int main()
{

}
